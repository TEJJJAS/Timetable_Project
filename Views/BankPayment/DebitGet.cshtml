@model CreditCardProj.Models.creditcombine






@{
    ViewData["Title"] = "Debit Details";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css">
    <style>
        .card {
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        }

        .card-header {
            background-color: #007bff;
            color: white;
            font-size: 1.5rem;
            text-align: center;
            border-bottom: 2px solid #0056b3;
        }

        .btn-primary-custom {
            background-color: #007bff;
            border-color: #007bff;
        }

            .btn-primary-custom:hover {
                background-color: #0056b3;
                border-color: #00409e;
            }
    </style>
</head>
<body>

    <div class="container mt-4">
        <div class="card">
            <div class="card-header">
                Debit Details
            </div>
            <div class="card-body">
                <form method="post">
                    <div class="mb-1\3">
                            <p class="lead"><strong>Bank Account ID: </strong>@Model.Bank.BankId</p>
                            @Html.HiddenFor(x => x.Bank.BankId)
                    </div>
                    <div class="mb-3">
                        <p class="lead">Account Holder: @Model.Bank.BankHolderName</p>
                        @Html.HiddenFor(x => x.Bank.BankHolderName)
                    </div>
                    <div class="mb-3">
                        <p class="lead">Account Number: @Model.Bank.BankAccount</p>
                        @Html.HiddenFor(x => x.Bank.BankAccount)
                    </div>
                    <div class="mb-3">
                        <p class="lead">Account Balance: @Model.Bank.BankActBalance</p>
                        @Html.HiddenFor(x => x.Bank.BankActBalance)
                    </div>
                    <div class="mb-3">
                        <p class="lead">Outstanding Balance: @Model.Monthly.OutstandingAmt</p>
                        @Html.HiddenFor(x => x.Monthly.OutstandingAmt)
                    </div>
                    <div class="mb-3">
                        <p class="lead">Billing Month: @Model.Monthly.InvoiceMonth</p>
                        @Html.HiddenFor(x => x.Monthly.InvoiceMonth)
                    </div></b>
                    <button type="submit" class="btn btn-primary btn-lg w-20">Confirm Payment</button>
                </form>
                <div class="mt-3">
                    @Html.ValidationSummary()
                </div>
            </div>
        </div>
    </div>

    <script src="https://stackpath.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>






@* <h2>Debit Details</h2>
<div>


    @using (Html.BeginForm())
    {

        <p>Bank Account ID: @Model.Bank.BankId</p>
        @Html.HiddenFor(x => x.Bank.BankId)
        ;
        <p>Account Holder: @Model.Bank.BankHolderName</p>
        @Html.HiddenFor(x => x.Bank.BankHolderName)
        ;
        <p>Account Number : @Model.Bank.BankAccount</p>
        @Html.HiddenFor(x => x.Bank.BankAccount)
        ;
        @Html.HiddenFor(x => x.Bank.BankActBalance)
        ;

        <p>Account Balance: @Model.Bank.BankActBalance</p>
        @Html.HiddenFor(x => x.Monthly.OutstandingAmt)
        ;
        <p>Outstanding Balance: @Model.Monthly.OutstandingAmt</p>
        @Html.HiddenFor(x => x.Monthly.OutstandingAmt)
        ;
        <p>Billing Month: @Model.Monthly.InvoiceMonth</p>
        @Html.HiddenFor(x => x.Monthly.InvoiceMonth)
        ;


        <button type="submit">Confirm Payment</button>
        <br />
        <div style="color: red; margin-top: 10px;">
            @Html.ValidationSummary()
            <span> </span>
        </div>
    }
</div> *@